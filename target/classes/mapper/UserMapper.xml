<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.online.messageboard.mapper.UserMapper">

    <!-- 结果映射：用户实体 -->
    <resultMap id="userResultMap" type="com.online.messageboard.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!-- 根据ID查询用户 -->
    <select id="selectById" resultMap="userResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="userResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <!-- 根据用户名和密码查询用户 -->
    <select id="selectByUsernameAndPassword" resultMap="userResultMap">
        SELECT * FROM user WHERE username = #{username} AND password = #{password}
    </select>

    <!-- 插入新用户 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, email, phone, create_time)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, #{phone}, #{createTime})
    </insert>
</mapper>
